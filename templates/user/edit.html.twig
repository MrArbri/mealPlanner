{% extends 'base.html.twig' %}

{% block title %}Update
{% endblock %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset("CSS/editadmCSS/style.css") }}">
{% endblock %}

{% block body %}
	<div class="mainContainer">
		<div class="container">
			<h1 class="mainTitle">Update Profile</h1>
			{{ include('user/_form.html.twig', {'button_label': 'Update profile'}) }}
			<div class="updateBtnHolder">
				{{ include('user/_delete_form.html.twig') }}
				<a class="backButton" href="{{ path('app_meal_index') }}">Back to meals</a>
			</div>
		</div>
	</div>
{% endblock %}
