<form method="post" action="{{ path('app_mealDashboard_delete', {'id': meal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meal.id) }}">
	<button class="btn btn-danger my-1">Delete</button>
</form>
