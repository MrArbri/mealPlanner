<link rel="stylesheet" href="{{asset("CSS/plannerCSS/style.css")}}">

<form method="post" action="{{ path('app_planner1_delete', {'id': planner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planner.id) }}">
	<button class="deleteButton">
		<i class="fa-solid fa-trash-can"></i>
		Delete</button>
</form>
