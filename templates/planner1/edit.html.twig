{% extends 'base.html.twig' %}

{% block title %}Edit Planner{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{asset("CSS/plannerCSS/style.css")}}">
{% endblock %}

{% block body %}
<div class="mainContainer">
  <div class="editContainer">
 
    <h1 class="text-center m-4">Edit Planner</h1>

    {{ include('meal/_plan.html.twig', {'button_label': 'Update'}) }}

    <a class="backBtn" href="{{ path('app_planner1_index') }}">Back to list</a>

    {{ include('planner1/_delete_form.html.twig') }}
  </div>
</div>
{% endblock %}
