{% extends 'base.html.twig' %}

{% block title %}Edit Planner
{% endblock %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{asset("CSS/plannerCSS/style.css")}}">
{% endblock %}

{% block body %}
	<div class="mainContainer">
		<div class="editContainer">

			<h1 class="mainTitle">Edit Planner</h1>

			{{ include('meal/_plan.html.twig', {'button_label': 'Update meal plan'}) }}

			<div class="editPlannerBtnHolder">
				{{ include('planner1/_delete_form.html.twig') }}
				<a class="backButton" href="{{ path('app_planner1_index') }}">Back to my planner</a>
			</div>
		</div>
	</div>
{% endblock %}
